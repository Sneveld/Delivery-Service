import { Component } from '@angular/core';
import { CartInfoComponent } from './CartInfo/cart-info.component';

@Component({
    selector: 'app',
    templateUrl: `./app.html`,
    providers: [CartInfoComponent]
})

export class AppComponent  {
   
}